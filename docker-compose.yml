services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fugue
      POSTGRES_PASSWORD: fugue
      POSTGRES_DB: fugue
    ports:
      - "5433:5432"
    volumes:
      - fugue_pgdata:/var/lib/postgresql/data

volumes:
  fugue_pgdata:
